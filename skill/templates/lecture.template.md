# Security Lecture — <PROGRAM_NAME>

> Generated by `solana-roast` (`/roast-lecture`). A teaching pass over the findings: what each bug
> class is, what breaks if it ships, and the real exploit it mirrors. Learn it once, never write it
> again. Sourced from the skill's exploit-library.md / SOURCES.md.

**Design Safety: <X>/10** — <C> CRITICAL, <H> HIGH, <M> MEDIUM, <L> LOW.

## What this program does (and where the risk is)

One paragraph: the program's job, what value it touches, and which one or two areas concentrate
the risk (so the reader knows where to be most careful).

---

## Lesson 1 — <Bug class> · <SEVERITY>

**The concept.** What this class of bug *is*, in plain language.

**In your code.** `<file:line>`
```rust
// the exact construct that has the flaw
```

**What breaks if unfixed.** The concrete failure, step by step:
1. Attacker does X …
2. The program accepts it because …
3. Result: <funds drained / auth bypassed / state corrupted>.
A tiny worked example with real numbers, not hand-waving.

**Real precedent.** This exact class cost **<Protocol> ~$<amount>** in **<date>** — <one-line
cause>. ([source](<url>)) *(If no clean precedent exists for this branch, say "taught from first
principles" and cite the Helius/Neodyme reference instead — do not force an attribution.)*

**The fix.**
```rust
// the corrected pattern
```
> Principle to remember: <one line>.

---

## Lesson 2 — <Bug class> · <SEVERITY>

(repeat the shape for each finding, CRITICAL/HIGH first)

---

## The 3 habits that would have prevented most of this

1. <principle 1 — e.g. "every privileged account is a `Signer` bound by `has_one`">
2. <principle 2 — e.g. "every value computation is `checked_*` with the invariant `require!`d">
3. <principle 3 — e.g. "every account you read is typed + constrained to its mint/authority">

## Where to go next

This lecture teaches the *design* lessons; it is not an audit. Next: Trail of Bits
`solana-vulnerability-scanner` → `review-and-iterate` → formal-verify the named invariants →
devnet → `deploy-to-mainnet`.

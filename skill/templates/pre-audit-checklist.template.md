# Pre-Audit Checklist — <PROGRAM_NAME>

> Generated by `solana-roast`. Hand this to an external auditor or the kit's audit/scanner
> skills — it tells them what was already designed-for, so they can focus on confirming it in
> code. Each item: `[x]` enforced, `[ ]` open, `[-]` N/A. Cite `file:line` where enforced.

## Accounts & PDAs
- [ ] Every data account read via typed `Account`/`InterfaceAccount` (owner + discriminator checked) — `<file:line>`
- [ ] All PDAs use canonical bump, stored at init, reused (no user-supplied bump) — `<…>`
- [ ] PDA seeds uniquely identify role; no cross-domain PDA sharing — `<…>`
- [ ] Account relationships enforced (`has_one` / key checks) — `<…>`
- [ ] Duplicate-mutable-account cases rejected — `<…>`
- [ ] `init` used (or `init_if_needed` proven re-init-safe) — `<…>`

## Authority & Signers
- [ ] Every privileged account is a `Signer` / `is_signer` verified (no key-only checks) — `<…>`
- [ ] Each signer bound to its resource (`has_one` / `require_keys_eq!`) — `<…>`
- [ ] Admin vs. user privilege separated — `<…>`
- [ ] Authority transfer is two-step / rejects default pubkey — `<…>`
- [ ] Complete instruction→authority access matrix exists (design-spec §4)

## CPI & Composability
- [ ] Every CPI target program ID is pinned/typed — `<…>`
- [ ] PDA signing scope is minimal / resource-scoped — `<…>`
- [ ] Account & signer/writable propagation reviewed — `<…>`
- [ ] State mutated before external CPIs (checks-effects-interactions) — `<…>`
- [ ] Token-2022 transfer hooks treated as reentrancy points — `<…>`

## State & Data
- [ ] Type discrimination enforced (no manual deserialize of program data) — `<…>`
- [ ] Re-initialization impossible / guarded — `<…>`
- [ ] Account close zeroes data + invalidates discriminator (revival-safe) — `<…>`
- [ ] `realloc` zeroes new bytes, correct payer, bounded — `<…>`
- [ ] `space` correct; all `Vec`/`String` bounded — `<…>`
- [ ] No trusted value taken as unchecked instruction argument — `<…>`

## Economic Invariants
- [ ] All invariants named and each has an enforcing line (design-spec §6) — `<…>`
- [ ] `overflow-checks = true` (release) + `checked_*` on value math — `<…>`
- [ ] Rounding always favors the protocol, consistently — `<…>`
- [ ] First-depositor / share-inflation mitigated — `<…>`
- [ ] Oracle staleness + confidence checked; no flash-manipulable spot price — `<…>`
- [ ] Slippage bounds (`min_out`/`max_in`) enforced — `<…>`
- [ ] Fees bounded (≤ cap) and inside the conservation invariant — `<…>`

## Upgrade & Governance
- [ ] Upgrade authority is multisig (not single hot key) or immutable-by-plan — `<…>`
- [ ] Immutable-vs-upgradeable decision is explicit
- [ ] Admin blast radius enumerated and bounded — `<…>`
- [ ] Multisig + timelock on highest-impact actions — `<…>`
- [ ] Emergency pause exists and never blocks user exit — `<…>`

## Compute, Limits & DoS
- [ ] No unbounded iteration; loops fit compute budget at max scale — `<…>`
- [ ] No account/collection grows without a close/cleanup path — `<…>`
- [ ] Tx size & account count within limits (or v0 + ALTs by design) — `<…>`
- [ ] Heavy instructions set compute limit; peak CU measured — `<…>`
- [ ] Caller (not protocol PDA) funds account rent; no squattable slots — `<…>`

## Tokens (SPL / Token-2022)
- [ ] SPL vs Token-2022 support decided; account types match — `<…>`
- [ ] Token accounts constrained by mint + authority — `<…>`
- [ ] Issued-token mint/freeze authority disposition deliberate — `<…>`
- [ ] Fee-on-transfer handled via balance delta — `<…>`
- [ ] Transfer-hook mints allow-listed or disallowed — `<…>`
- [ ] Decimals read per-mint; `*_checked` transfer variants used — `<…>`

## Process
- [ ] Tests on LiteSVM/Mollusk + `anchor test` cover every CRITICAL/HIGH finding
- [ ] Deployed and exercised on devnet
- [ ] External audit scheduled/complete
- [ ] Verified build published

---

**Reminder:** a green checklist reduces design risk; it does **not** replace a professional
audit. See the threat-model's "Bottom Line" for the hand-off path.

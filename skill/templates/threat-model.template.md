# Threat Model — <PROGRAM_NAME>

> Generated by `solana-roast`. Every finding from the interrogation, triaged by severity, with
> the decision taken and residual risk. **This is a design-stage threat model, not an audit.**
>
> Date: <YYYY-MM-DD> · Branches run: <list> · Findings: <C>/<H>/<M>/<L>

## Design Safety: <X>/10

Computed from 10 − (3·CRITICAL + 2·HIGH + 1·MEDIUM + 0.5·LOW), floored at 1. **A high score
reflects reduced *design* risk only — it is not an audit pass.**

## Severity Summary

| Severity | Count | Accepted (will fix) | Deferred | Won't fix (risk accepted) |
|----------|-------|---------------------|----------|---------------------------|
| CRITICAL | | | | |
| HIGH | | | | |
| MEDIUM | | | | |
| LOW | | | | |

## Findings Ledger

| ID | Branch | Severity | Finding | Exploit class | Decision | Status |
|----|--------|----------|---------|---------------|----------|--------|
| F-01 | authority | CRITICAL | `withdraw` authority is `AccountInfo`, never signs | Wormhole / missing-signer | switch to `Signer` + `has_one` | accepted |
| F-02 | pdas | HIGH | bump re-derived per ix, not stored | non-canonical bump | store + reuse canonical bump | accepted |
| F-03 | economic | MEDIUM | fee math rounds toward user | rounding leak | round toward protocol | accepted |

## Detailed Findings

### F-01 — <title> · CRITICAL
- **Where:** `<file:line>`
- **What:** <one-paragraph description>
- **Why it matters:** <blast radius; the real exploit it maps to>
- **Recommended fix:** <concrete change>
- **Decision:** <accepted / deferred / wontfix + rationale>

(repeat per finding)

## Trust Assumptions (residual risk)

What users must trust even after all accepted fixes — e.g. "admin multisig signers are honest",
"Pyth feed is live", "upgrade authority is not compromised". State these plainly; they are the
project's honest risk disclosure.

## Invariants for Formal Verification

The named economic invariants (branch 5) suitable for the QEDGen / Lean-4 hand-off:

1. `<invariant>` — informal statement + the on-chain enforcement point.
2. ...

## Bottom Line

> Design risk has been materially reduced across <N> branches. **This is not a security audit.**
> Proceed to: pattern scan (Trail of Bits `solana-vulnerability-scanner`) → deep audit
> (`solana-auditor` / `review-and-iterate`) → formal verification of the invariants above →
> devnet → `deploy-to-mainnet`.

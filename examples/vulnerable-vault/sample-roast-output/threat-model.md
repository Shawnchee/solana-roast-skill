# Threat Model — vulnerable-vault

> Generated by `solana-roast`. Findings from the interrogation, triaged by severity, with the
> decision taken. **This is a design-stage threat model, not an audit.**
>
> Date: 2026-06-21 · Branches run: 1,2,3,4,5,6,8 (9 N/A — no frontend) · Findings: 5 CRITICAL / 2 HIGH / 1 MEDIUM

## Design Safety: 1/10

10 − (3·5 + 2·2 + 1·1) = −10, floored at 1. Trivially drainable as written — see the lecture for
why. **A high score would reflect reduced *design* risk only; it is never an audit pass.**

## Severity Summary

| Severity | Count | Accepted (will fix) | Deferred | Won't fix |
|----------|-------|---------------------|----------|-----------|
| CRITICAL | 5 | 5 | 0 | 0 |
| HIGH | 2 | 2 | 0 | 0 |
| MEDIUM | 1 | 1 | 0 | 0 |
| LOW | 0 | — | — | — |

## Findings Ledger

| ID | Branch | Severity | Finding | Exploit class | Decision | Status |
|----|--------|----------|---------|---------------|----------|--------|
| F-01 | authority | CRITICAL | `withdraw.authority` is `UncheckedAccount`, not a `Signer`, not bound to `vault.authority` | missing-signer / auth bypass | `Signer` + `has_one = authority` | accepted |
| F-02 | cpi | CRITICAL | `withdraw.token_program` unverified → arbitrary CPI | arbitrary CPI | typed `Interface<TokenInterface>` | accepted |
| F-03 | tokens | CRITICAL | token accounts lack `token::mint`/`token::authority` constraints | token-account substitution | constrain mint + authority | accepted |
| F-04 | economic | CRITICAL | unchecked `+`/`-` on `total_deposited`; withdraw underflows | integer overflow/underflow | `checked_*` + `require!(amount<=total)` | accepted |
| F-07 | economic | CRITICAL | `overflow-checks = false` in release (enables F-04) | overflow (process) | set `overflow-checks = true` | accepted |
| F-05 | accounts/pdas | HIGH | bump taken as instruction argument | non-canonical bump | use Anchor bare `bump` / stored canonical | accepted |
| F-06 | governance | HIGH | upgradeable with no documented authority plan | upgrade-key rug | Squads v4 multisig + timelock | accepted |
| F-08 | tokens | MEDIUM | deposit credits requested amount, not actual delta (Token-2022 fee mints) | fee-on-transfer over-credit | credit balance delta / allow-list mints | accepted |

## Detailed Findings

### F-01 — withdraw is unauthenticated · CRITICAL
- **Where:** `programs/vault/src/lib.rs`, `Withdraw` accounts struct.
- **What:** `authority: UncheckedAccount` gates nothing — it never signs and is never compared to
  `vault.authority`. Any caller can invoke `withdraw`.
- **Why it matters:** Direct, trivial drain of the entire vault. The single worst bug here.
- **Fix:** `authority: Signer<'info>` + `#[account(mut, has_one = authority)]` on `vault`.
- **Decision:** accepted.

### F-02 — arbitrary CPI via unverified token program · CRITICAL
- **Where:** `Withdraw.token_program: UncheckedAccount`, used in `CpiContext::new_with_signer`.
- **What:** The vault PDA signs a CPI into a caller-supplied program.
- **Why it matters:** Attacker substitutes their own program and receives a PDA-signed call.
- **Fix:** `Interface<'info, TokenInterface>` (supports SPL + Token-2022) or `Program<Token>`.
- **Decision:** accepted (Token-2022 support requested → Interface).

### F-03 — unconstrained token accounts · CRITICAL
- **Where:** `deposit.vault_token_account`, `withdraw.vault_token_account`, `withdraw.destination`.
- **What:** No `token::mint` / `token::authority` constraints.
- **Why it matters:** Wrong-mint or attacker-owned token accounts pass validation → mis-accounting / theft.
- **Fix:** constrain to the expected mint and the vault PDA authority.
- **Decision:** accepted.

### F-04 / F-07 — unchecked math + overflow-checks off · CRITICAL
- **Where:** `deposit` (`+`), `withdraw` (`-`); `Cargo.toml [profile.release] overflow-checks = false`.
- **What:** `withdraw` underflows `total_deposited` to a huge u64; release builds wrap.
- **Why it matters:** Accounting can be driven to nonsense; combined with F-01/F-03 it's a clean drain.
- **Fix:** `overflow-checks = true`, `checked_add`/`checked_sub`, and `require!(amount <= total_deposited)`.
- **Decision:** accepted.

### F-05 — user-supplied PDA bump · HIGH
- **Where:** `initialize(vault_bump: u8)` → `vault.bump = vault_bump`.
- **What:** Caller controls the stored bump; a non-canonical valid bump can be supplied.
- **Why it matters:** Enables alternate valid addresses / weakens PDA guarantees.
- **Fix:** use Anchor's bare `bump` at init and `bump = vault.bump` after; drop the argument.
- **Decision:** accepted.

### F-06 — upgrade authority undocumented · HIGH
- **Where:** program config / deploy (not code).
- **What:** Upgradeable with no stated authority custody plan.
- **Why it matters:** A single hot upgrade key can replace the whole program — dominant rug vector.
- **Fix:** Squads Protocol v4 multisig + timelock; publish policy; path to immutability.
- **Decision:** accepted.

### F-08 — Token-2022 fee-on-transfer over-credit · MEDIUM
- **Where:** `deposit` credits `amount` rather than the received delta.
- **What:** Transfer-fee mints deliver less than `amount`; crediting `amount` over-credits.
- **Why it matters:** Drainable when fee-bearing mints are allowed.
- **Fix:** credit `balance_after - balance_before`, or allow-list non-fee mints.
- **Decision:** accepted.

## Trust Assumptions (residual risk after fixes)

- The Squads v4 signer set is honest / keys uncompromised.
- The upgrade authority is not compromised before immutability.
- Any allowed Token-2022 mint's extensions (hooks/fees) are within the documented policy.

## Invariants for Formal Verification

1. `total_deposited == Σ(net deposits)` and `total_deposited ≤ vault_token_balance` at all times.
   → hand off to QEDGen Lean-4 (`npx skills add qedgen/solana-skills`).

## Bottom Line

> Design risk reduced from "trivially drainable by anyone" to "standard custodial vault pending
> audit." **This is not a security audit.** Proceed: Trail of Bits `solana-vulnerability-scanner`
> → `review-and-iterate` → formal-verify the invariant above → devnet → `deploy-to-mainnet`.

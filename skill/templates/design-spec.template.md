# Design Spec — <PROGRAM_NAME>

> Generated by `solana-roast`. The resolved, agreed design after interrogation. This is the
> single source of truth for implementation — every instruction's checks are specified here.
>
> Date: <YYYY-MM-DD> · Program: <PROGRAM_ID or "unset"> · Anchor: <version> · Network target: <devnet/mainnet>

## 1. Summary

One paragraph: what the program does, what value it custodies, and its core trust assumption.

## 2. Account Model

| Account | Type | Owner program | Created by | Writable by | Notes |
|---------|------|---------------|------------|-------------|-------|
| Config | PDA | this program | admin (init) | admin | singleton |
| Vault | PDA token acct | this program | admin | this program (signed) | holds SPL `<mint>` |
| Position | PDA | this program | user | user / liquidator | per-user |

## 3. PDA Map

| PDA | Seeds | Canonical bump stored? | Signing authority for | Role (one only) |
|-----|-------|------------------------|------------------------|-----------------|
| Config | `["config"]` | yes (`config.bump`) | config updates | global config |
| Vault auth | `["vault", market.key()]` | yes | vault token transfers | this market only |

## 4. Authority Model (Access Matrix)

| Instruction | Caller | Required signer | Bound to (has_one / key check) | Mutates | Post-invariant |
|-------------|--------|-----------------|--------------------------------|---------|----------------|
| `initialize` | deployer | `admin: Signer` | — | Config | config set once |
| `deposit` | user | `owner: Signer` | `has_one = owner` | Position, Vault | conservation holds |
| `withdraw` | user | `owner: Signer` | `has_one = owner` | Position, Vault | no over-withdraw |
| `set_fee` | admin | `admin: Signer` | `has_one = admin` | Config | fee ≤ MAX_FEE |

## 5. Token Model (if applicable)

- Token program(s) supported: <SPL / Token-2022 / both>
- Issued token mint authority: <revoked / multisig + schedule>
- Issued token freeze authority: <None / documented reason>
- Token-2022 extensions allowed: <none / fee / hook — with handling>

## 6. Economic Invariants

1. `<invariant 1>` — enforced at `<file:line>`
2. `<invariant 2>` — enforced at `<file:line>`
3. ...

(These feed the formal-verification hand-off.)

## 7. Governance

- Upgrade authority (launch → target): <hot wallet → Squads M-of-N + timelock → immutable>
- Admin powers (bounded?): see access matrix; caps: <fee ≤ X, oracle allow-list, …>
- Emergency pause: <design; never blocks user exit>

## 8. Open Design Questions

Anything the user deferred or hasn't decided. Each is a blocker to resolve before audit.
